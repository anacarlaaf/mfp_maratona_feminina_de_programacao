#include <iostream>
#include <algorithm>
#include <vector>
using namespace std;

int main() {
    int n, q, qn;
    vector<int> lista;
    cin >> n, q;
    cin >> lista;

    

    for (int i = 0; i < q; i++) {
        cin >> qn;

    }    
}